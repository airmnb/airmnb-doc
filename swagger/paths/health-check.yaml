/health-check:
  get:
    tags:
      - func
    summary: For load balancer health check
    responses:
      '200':
        description: successful login
        content:
          application/json:
            schema:
              type: string
